machine:
  timezone:
    America/New_York

dependencies:
  override:
   - echo "No tests are needed"

test:
  override:
    - echo "No tests are needed"

deployment:
  staging:
    branch: dev
    commands:
      - ssh $SSHU "source ~/.profile && cd $DEVDIR && git pull origin dev && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/npm install && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/npm run build && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/pm2 restart server.dev"
  production:
    branch: master
    commands:
      - ssh $SSHU "source ~/.profile && cd $DIR && git pull origin master && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/npm install && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/npm run build && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/pm2 restart server.prod"