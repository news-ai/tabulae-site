general:
  branches:
    only:
      - master

machine:
  timezone:
    America/Los_Angeles

dependencies:
  override:
   - echo "No tests are needed"

test:
  override:
    - echo "No tests are needed"

deployment:
  production:
    branch: master
    commands:
      - ssh $SSHU "cd $DIR && git pull origin master && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/npm install && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/npm run build && /home/abhiagarwal/.nvm/versions/node/v5.10.1/bin/forever restartall"
